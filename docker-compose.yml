version: '2'

services:
  node1:
    build: .
    container_name: baobaxia_node1
    hostname: node1
    environment:
      - TERM=xterm-256color
    networks:
      - baobaxia
    # volumes:
    #   - ../baobaxia:/opt/baobaxia

  node2:
    build: .
    container_name: baobaxia_node2
    hostname: node2
    environment:
      - TERM=xterm-256color
    networks:
      - baobaxia
    # volumes:
    #   - ../baobaxia:/opt/baobaxia

networks:
  baobaxia:
